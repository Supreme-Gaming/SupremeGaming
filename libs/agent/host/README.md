# agent-host

Host-process runtime (`@supremegaming/agent/host`): command dispatcher, BullMQ jobs, heartbeat, and credentials.

The process shell is `apps/supreme-host-agent-node`. Shared protocol types live in [`@supremegaming/agent/core`](../core/README.md).

## Tests

```bash
nx test agent-host
```
