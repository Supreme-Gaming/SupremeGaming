import { AtlasGridRowIndexPipe } from './atlas-grid-row-index.pipe';

describe('AtlasGridRowIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new AtlasGridRowIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
