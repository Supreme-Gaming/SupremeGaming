import { AtlasGridColumnIndexPipe } from './atlas-grid-column-index.pipe';

describe('AtlasGridColumnIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new AtlasGridColumnIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
